[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install fastapi uvicorn[standard] pydantic python-dotenv openai"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
